# All Your Handover 环境变量配置
# 复制此文件为 .env.local 并填写实际值

# 服务端口（默认 3000）
PORT=3000

# 数据目录（默认 ./data）
DATA_DIR=./data

# 配置加密密钥（可选，不设置则自动生成并保存到 data/config/.encryption-key）
# ENCRYPTION_KEY=

# 管理后台认证 Token（生产环境必须设置，不设置则无认证保护）
# ADMIN_TOKEN=