# All Your Handover - Windows 服务环境配置
# 修改后需重启服务: nssm restart AllYourHandover

# 服务端口 (默认: 3000)
PORT=3000

# 数据目录 (默认: ./data，相对于项目根目录)
DATA_DIR=./data

# 加密密钥 - API Key 等敏感配置加密用 (可选，不设置则自动生成)
# ENCRYPTION_KEY=

# 管理后台认证 Token (生产环境必须设置，否则管理后台无认证保护)
# ADMIN_TOKEN=